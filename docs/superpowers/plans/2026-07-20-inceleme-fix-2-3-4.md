# Kod İncelemesi Fix Planı (Bulgu 2, 3, 4)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). TDD, checkbox adımlar.

**Goal:** Kod-inceleme bulguları #2 (BackgroundService host çökmesi), #3 (slug yarışı → 500), #4 (purge fiziksel dosya artığı) düzeltilir; her biri gerileme testiyle kilitlenir.

**Dal:** `main`'den `fix/review-2-3-4`. Commit sonu `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## Fix #2 — IntervalHours ≤ 0 host'u çökertiyor

**Kök neden:** `UnverifiedTenantCleanupService.ExecuteAsync`'te `new PeriodicTimer(TimeSpan.FromHours(_options.IntervalHours))` try/catch dışında; `IntervalHours ≤ 0` → ctor fırlatır → BackgroundService varsayılanı `StopHost` → API başlamaz.

**Yaklaşım:** Enabled kontrolünün hemen ardından `IntervalHours ≤ 0` guard'ı: hata logla, dön (temizlik devre dışı, host ayakta).

- [ ] **Test:** `ScheduledCleanupTests.Start_WithNonPositiveInterval_DoesNotThrow`: `Enabled=true, IntervalHours=0` ile servisi kur, `IHostedService.StartAsync` fırlatmamalı (fix öncesi fırlatır).
- [ ] **Kod:** guard ekle.
- [ ] Test PASS.

## Fix #3 — Slug kontrol-sonra-ekle yarışı → 500

**Kök neden:** `RegisterTenantCommandHandler` slug'ı `AnyAsync` ile kontrol edip ekliyor; eşzamanlı iki kayıt aynı slug'ı hesaplayıp ikincisi unique index'i ihlal eder → yakalanmamış `DbUpdateException` (500).

**Yaklaşım:** Kiracı yazımını sınırlı (5) yeniden-deneme döngüsüne al; `DbUpdateException`'da izlenen eklemeyi `context.Tenants.Remove` ile bırak, yeniden denemede kısa rastgele ekli slug türet (hızlı yakınsama). Register artık `TenantOnboarding` yerine kendi retry'li akışını kullanır (provizyon helper'ı değişmez).

- [ ] **Test:** `SelfServiceSignupTests.Signup_ConcurrentSameCompanyName_AllSucceedWithDistinctSlugs`: aynı şirket adıyla 4 eşzamanlı signup (farklı e-posta) → hepsi 201, slug'lar benzersiz.
- [ ] **Kod:** handler'ı retry'li yaz.
- [ ] Test PASS + mevcut signup testleri yeşil.

## Fix #4 — Purge fiziksel dosya artığı (KVKK)

**Kök neden:** `TenantPurger.PurgeAsync` DB commit sonrası dosyaları döngüde siler; bir `DeleteAsync` fırlarsa döngü kalan dosyaları silmeden çıkar ve metod fırlar (DB silinmiş ama diskte PII kalır, çağırana 500).

**Yaklaşım:** `ILogger<TenantPurger>` inject et; her dosya silmeyi ayrı try/catch'e al, hatayı logla, devam et; sonda başarısız sayısını uyarı logla. Böylece silinebilen tüm dosyalar silinir, hatalar sessizce yutulmaz ve işlem yarıda fırlamaz.

- [ ] **Test:** `TenantPurgeTests.Purge_DeletesPhysicalDocumentFiles`: kiracıya gerçek dosyalı `EmployeeDocument` seed'le; purge sonrası fiziksel dosya diskte YOK.
- [ ] **Kod:** try/catch + log.
- [ ] Test PASS.

## Kapanış
- [ ] Tam backend paketi + docs (KVKK Bölüm 7.1'e purge dayanıklılık notu). Commit + kullanıcı onayıyla merge/push.
